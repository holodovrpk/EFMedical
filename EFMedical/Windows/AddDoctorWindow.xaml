<Window x:Class="EFMedical.Windows.AddDoctorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить врача"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        Background="#f8f9fb">

    <Border Padding="20" CornerRadius="10" Background="White" BorderBrush="#d0d4dc" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="Добавление врача"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <!-- Поля ввода -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="ФИО:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
                <TextBox Text="{Binding FullName}" Grid.Row="0" Grid.Column="1" Margin="5" Width="200"/>

                <TextBlock Text="Специальность:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <TextBox Text="{Binding Specialty}" Grid.Row="1" Grid.Column="1" Margin="5,10,5,0" Width="200"/>

                <TextBlock Text="Телефон:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0"/>
                <TextBox Text="{Binding Phone}" Grid.Row="2" Grid.Column="1" Margin="5,10,5,0" Width="200"/>
            </Grid>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Сохранить" Width="100" Margin="5" Click="Save_Click"/>
                <Button Content="Отмена" Width="100" Margin="5" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
